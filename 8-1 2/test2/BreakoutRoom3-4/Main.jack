class Main {
    function void main() {
        var int x;
        let x = Main.max(10,20);
        do Main.print(x);
        return;
    }
    function int max(int x, int y) {
        if (x > y) {
            return x;
        } else {
            return y;
        }
    }
    function void print(int x) {
        do Output.printInt(x);
        do Output.println();
        return;
    }
}