class Main {
    function void main() {
        var int h, w, BMI;
        let h = Keyboard.readInt("input height (cm): ");
        let w = Keyboard.readInt("input weight (kg): ");
        let BMI = w * 100 / h * 100 / h;
        do Output.printString("BMI = ");
        do Output.printInt(BMI);
        do Output.println();
        return;
    }
}